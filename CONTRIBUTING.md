# Contributing

Bug reports and pull requests are welcome through GitHub issues. Please run `pytest` before opening a
pull request. Data files are versioned; if you find an error in a per-ligand value, open an issue with the
`ligand_id` and the evidence rather than editing the CSV directly, so the correction can be propagated to
all derived tables and documented in the changelog.
